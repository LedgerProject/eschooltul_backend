<!DOCTYPE html>
<html>
  <head>
    <title>ESCHOOLTUL</title>
    <%= render "layouts/assets" %>
  </head>
  <body>
    <div class="w-full flex flex-col min-h-screen">
      <div class="wrap min-h-screen flex relative" x-data="{sidebarOpen: false}"> 
        <%= render "layouts/sidebar" %>
        <div class="main">
          <%= render "layouts/header" %>
          <div class="main-content"><%= yield %></div>
        </div>
      </div>
    </div>
  </body>
</html>
