<%= simple_form_for(@term, url: form_url) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <%= f.input :name %>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
